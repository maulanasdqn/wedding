import { type FC, type ReactElement } from "react";

export const InvitationView: FC = (): ReactElement => {
  return (
    <section className="flex justify-center w-full h-[300px] bg-white bg-opacity-40 rounded-md text-yellow-300 text-3xl">
      <h1>Wedding of</h1>
    </section>
  );
};

export default InvitationView;
